<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wannabe.app.main.mapper.CommentMapper">
	<resultMap id="Comment" type="com.wannabe.app.main.data.entity.Comment">
		<id column="id" property="id"/>
		<result column="article_id" property="articleId"/>
		<result column="user_id" property="userId"/>
		<result column="parent_id" property="parentId"/>
		<result column="content" property="content"/>
		<result column="date_created" property="createAt" typeHandler="com.wannabe.app.main.utility.mybatis.LocalDateTimeHandler"/>
		<result column="date_updated" property="updateAt" typeHandler="com.wannabe.app.main.utility.mybatis.LocalDateTimeHandler"/>
		<result column="reply" property="isReply"/>
		<result column="deleted_yn" property="isDeleted"/>
	</resultMap>

	<select id="findAllComment" parameterType="Map" resultMap="Comment">
		SELECT *
		FROM t_comment
		WHERE article_id = #{articleId}
		  AND reply = 'N'
		ORDER BY date_created DESC
		LIMIT #{size} OFFSET #{page} * 10;
	</select>

	<select id="findAllReply" parameterType="long" resultMap="Comment">
		SELECT *
		FROM t_comment
		WHERE parent_id = #{parentId}
		  AND reply = 'Y'
		<!--ORDER BY date_created DESC-->
		ORDER BY id
	</select>

	<insert id="createComment" parameterType="Map" useGeneratedKeys="true">
		INSERT INTO t_comment (id, article_id, user_id, parent_id, content, reply)
		VALUES (#{id}, #{articleId}, #{userId}, #{commentRequest.commentId}, #{commentRequest.content}, #{commentRequest.isReply})
	</insert>

	<select id="findCommentIdSequence" resultType="long">
		select nextval('comment_id_seq'::regclass);
	</select>

	<select id="countAll" parameterType="long" resultType="long">
		SELECT COUNT(*)
		FROM t_comment
		WHERE article_id = #{articleId};
	</select>

	<select id="findById" parameterType="Map" resultMap="Comment">
		SELECT *
		FROM t_comment
		WHERE id = #{commentId}
		  AND user_id = #{userId};
	</select>

	<select id="findByIdAndArticleId" parameterType="Map" resultMap="Comment">
		SELECT *
		FROM t_comment
		where article_id = #{articleId}
		  AND id = #{commentId}
	</select>

	<select id="findByIdAndUserId" parameterType="Map" resultMap="Comment">
		SELECT *
		FROM t_comment
		where article_id = #{articleId}
		  AND id = #{commentId}
		  AND user_id = #{userId}
	</select>

	<update id="updateComment" parameterType="Map" useGeneratedKeys="true">
		UPDATE t_comment
		SET content = #{content}
		WHERE id = #{commentId}
	</update>

	<update id="deleteComment" parameterType="Map">
		UPDATE t_comment
		SET deleted_yn = #{state}
		WHERE id = #{commentId}
	</update>
</mapper>
