<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wannabe.app.main.mapper.AnnouncementMapper">

    <select id="getAnnouncementsActive" resultType="com.wannabe.app.main.data.entity.Announcement">
        /* com.wannabe.app.main.mapper.AnnouncementMapper.getAnnouncementsActive*/
        select
            a.id ,
            a.title,
            a.description ,
            a.image_group_id
        from
            t_admin_forum a
        where
            1 = 1
            and a.type = 'ANNOUNCEMENT'
            and a.state = 'ACTIVE'
    </select>
</mapper>
